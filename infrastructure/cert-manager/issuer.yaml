apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-nexuscore-production
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: secops@nexuscore.network
    privateKeySecretRef:
      name: letsencrypt-production-account-key
    solvers:
      - http01:
          ingress:
            class: nginx
